# Calcit 0.13.29 upgrade

- Rebases the upgrade branch onto current `main` and updates Calcit/procs dependencies to 0.13.29.
- Migrates CI to `setup-calcit` and current `calcit` commands, creates report directories before analyzer output, and gates deployment to pushes on `main`.
- Keeps the canonical `calcit.cirru` snapshot and validates the existing quality baseline.
- Verified dependency resolution, immutable Yarn install, check-only, analyzer baseline, and JS codegen locally.
